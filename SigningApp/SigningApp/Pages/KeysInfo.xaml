<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:viewmodel="clr-namespace:XamarinLicentaApp.ViewModel" x:DataType="viewmodel:KeysInfoViewModel"
             x:Class="XamarinLicentaApp.Pages.KeysInfo">
    <ContentPage.Content>
        <StackLayout>
            <Picker Title="Select a key to display info"
                    TextColor="Black"
                    ItemsSource="{Binding KeyName}" />
            <Label Text="{Binding Key.authMode}" 
                   HorizontalOptions="Center"  />

        </StackLayout>
    </ContentPage.Content>
</ContentPage>